# ══════════════════════════════════════════════════════════════
# ENVIRONMENT VARIABLES TEMPLATE
# ══════════════════════════════════════════════════════════════
# 
# This file shows what environment variables the project needs.
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# In production, use strong passwords and never commit .env to git!
# ══════════════════════════════════════════════════════════════


# ══════════════════════════════════════════════════════════════
# RABBITMQ CONFIGURATION
# Message broker for communication between services
# ══════════════════════════════════════════════════════════════

RABBITMQ_HOST=rabbitmq          # Hostname of RabbitMQ server
                                # Use "rabbitmq" for Docker (container name)
                                # Use "localhost" if running RabbitMQ locally

RABBITMQ_PORT=5672              # AMQP protocol port (default: 5672)
                                # This is for message sending, not the web UI

RABBITMQ_USER=guest             # Username for RabbitMQ authentication
                                # "guest" only works for localhost connections

RABBITMQ_PASS=guest             # Password for RabbitMQ authentication
                                # In production: use a strong password!


# ══════════════════════════════════════════════════════════════
# POSTGRESQL CONFIGURATION
# Database for storing videos, violations, and ROI configs
# ══════════════════════════════════════════════════════════════

POSTGRES_HOST=postgres          # Hostname of PostgreSQL server
                                # Use "postgres" for Docker (container name)
                                # Use "localhost" if running PostgreSQL locally

POSTGRES_PORT=5432              # PostgreSQL default port

POSTGRES_DB=violations_db       # Name of the database to use
                                # Will be created automatically on first run

POSTGRES_USER=postgres          # Database username
                                # "postgres" is the default superuser

POSTGRES_PASSWORD=postgres123   # Database password
                                # In production: use a strong password!


# ══════════════════════════════════════════════════════════════
# VIDEO CONFIGURATION
# Settings for the Frame Reader service
# ══════════════════════════════════════════════════════════════

VIDEO_PATH=/videos/sample.mp4   # Path to the video file to process
                                # This path is INSIDE the Docker container
                                # Maps to ./videos/sample.mp4 on your machine


# ══════════════════════════════════════════════════════════════
# MODEL CONFIGURATION
# Settings for the YOLO detection model
# ══════════════════════════════════════════════════════════════

MODEL_PATH=/app/models/yolo_model.pt    # Path to YOLO model file
                                        # This path is INSIDE the Docker container

CONFIDENCE_THRESHOLD=0.5        # Minimum confidence for detections (0.0 to 1.0)
                                # Higher = fewer but more confident detections
                                # Lower = more detections but more false positives


# ══════════════════════════════════════════════════════════════
# ROI CONFIGURATION (Default values)
# Region of Interest - the area we monitor for violations
# These can be changed via the API at runtime
# ══════════════════════════════════════════════════════════════

ROI_X1=100                      # Left edge of ROI (pixels from left)
ROI_Y1=150                      # Top edge of ROI (pixels from top)
ROI_X2=400                      # Right edge of ROI (pixels from left)
ROI_Y2=400                      # Bottom edge of ROI (pixels from top)
                                # These define a rectangle: (100,150) to (400,400)


# ══════════════════════════════════════════════════════════════
# STREAMING SERVICE CONFIGURATION
# Settings for the FastAPI backend
# ══════════════════════════════════════════════════════════════

STREAMING_HOST=0.0.0.0          # Host to bind to
                                # 0.0.0.0 = accept connections from anywhere
                                # 127.0.0.1 = only accept local connections

STREAMING_PORT=8000             # Port for the FastAPI server
                                # Access API at: http://localhost:8000
                                # Access docs at: http://localhost:8000/docs
